# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0

# AI/ML libraries (add as needed)
# openai>=1.0.0
# anthropic>=0.7.0
# transformers>=4.35.0
# pillow>=10.0.0

# Utilities
# Note: pydantic is installed by gradio, using compatible version range
pydantic>=2.0.0,<3.0.0

# GUI
# Note: gradio brings in httpx, uvicorn, and other dependencies
# Let gradio manage its own dependency versions
gradio>=4.0.0
